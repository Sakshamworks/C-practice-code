#include <stdio.h>
float main()
{
    float a;
    printf("enter how much dollar covert in rupees :");
    scanf("%f", &a);
    printf("after converting doller in rupees %.2f\n : ", a * 92.60);
    return 0;
}